{% extends "node.html.twig" %}
{#
/**
 * @file
 * NJALA2020's theme implementation to display a Student node.
 */
#}
{% if content.field_nu_stdt_photo|render and content.field_nu_stdt_body|render %}
  {% set two_column_layout = true %}
  {% set top_column_class_first = 'col-sm-4 col-md-5 order-sm-last pl-lg-5' %}
  {% set top_column_class_second = 'col-sm-8 col-md-7 order-sm-first' %}
  {% set bottom_column_class = 'col-sm-8 col-md-7 order-sm-first' %}
{% else %}
  {% set two_column_layout = false %}
  {% set top_column_class_first = 'col-12 col-xl-10' %}
  {% set top_column_class_second = 'col-12 col-xl-10' %}
  {% set bottom_column_class = 'col-12 col-xl-10' %}
{% endif %}

{% block content %}
  {{ attach_library('njala2020/node-student') }}
  <div class="row content-top mb-sm-3">
    {% if content.field_nu_stdt_photo|render %}
      {% if page %}
        {{ attach_library('njala2020/magnific-popup-field-nu-stdt-photo-init') }}
      {% endif %}
      <div class="{{ top_column_class_first }}">
        <div class="photo-container mb-4 mb-sm-0">
          {{ content.field_nu_stdt_photo }}
        </div>
      </div>
    {% endif %}
    <div class="{{ top_column_class_second }}">
      {{ content.field_nu_stdt_academic_title }}
      {{ content.field_nu_stdt_body }}
      {{ content.links }}
    </div>
  </div>
  <div class="row content">
    <div class="{{ bottom_column_class }}">
      {{ content|without('field_nu_stdt_photo','field_nu_stdt_body','field_nu_stdt_academic_title','links','field_nu_stdt_banner_image') }}
    </div>
  </div>
{% endblock %}
